repo_id: beastf
dataset_path: $TMPDIR/banana_beso_all
task: "put the banana in the bowl"
checkpoint_path: /hkfs/work/workspace/scratch/unisc-beso/output

train:
  batch_size: 1 #64
  steps: 3000 #40000
  save_freq: 1000
  seed: 42
  device: cuda
  num_workers: 0
  log_freq: 100
  output_dir: "./output/beastf/"
  job_name: "beastf_training"
  push_to_hub: false
  #Only pi0/pi05
  gradient_checkpointing: true
  compile_model: false

wandb:
  enable: True
  project: "beastf-smoke-training"
  entity: "jonasjiang26-karlsruhe-institute-of-technology"
  mode: "online"  # "online", "offline", "disabled"
